# TEMPLATE — never commit real values (PLAN.md section 5). Populate from
# Secret Manager at deploy time instead of editing this file:
#
#   kubectl create secret generic llm-api-key \
#     --from-literal=OPENROUTER_API_KEY="$(gcloud secrets versions access latest \
#       --secret=leadtech-ats-llm-api-key)"
#
apiVersion: v1
kind: Secret
metadata:
  name: llm-api-key
type: Opaque
stringData:
  OPENROUTER_API_KEY: "REPLACE_ME_VIA_KUBECTL_CREATE_SECRET"
